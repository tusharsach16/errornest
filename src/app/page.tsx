export default function HomePage() {
  return (
    <main className="mx-auto max-w-[1280px] px-6 py-16">
      <h1 className="text-3xl font-semibold tracking-tight">
        Catch production errors before your users report them
      </h1>
      <p className="mt-4 max-w-[680px] text-base text-gray-600">
        ErrorNest groups, tracks, and alerts on real errors from your app —
        signup and drop in your first project to see it live.
      </p>
    </main>
  );
}
